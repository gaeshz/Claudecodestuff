# Copy to .env and fill in real values. Never commit .env.
# Tools load these via python-dotenv.

# Example keys — add what your tools actually need:
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GOOGLE_APPLICATION_CREDENTIALS=credentials.json
